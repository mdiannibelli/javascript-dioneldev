// Crear una función que devuelva la tabla de multiplicación de un número

function multiplicar(num) {
  if(num <= 0) return 0;
  const nros = []
  for(let i = 1; i < 11; i++) {
    nros.push(num * i)
  }
  
  return nros;
}

const $num = 2;
multiplicar($num)